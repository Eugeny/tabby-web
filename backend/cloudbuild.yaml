steps:
- name: 'gcr.io/cloud-builders/docker'
  dir: 'backend'
  args:
  - build
  - '-t'
  - '${_DOCKER_PUBLISH_URL}'
  - '--cache-from'
  - '${_DOCKER_PUBLISH_URL}'
  - '.'

images: ['${_DOCKER_TAG}']
