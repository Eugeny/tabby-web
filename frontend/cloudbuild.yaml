steps:
- name: 'gcr.io/cloud-builders/docker'
  dir: 'frontend'
  args:
  - build
  - '-t'
  - '${_DOCKER_PUBLISH_URL}'
  - '--cache-from'
  - '${_DOCKER_PUBLISH_URL}'
  - '--build-arg'
  - 'BACKEND_URL=${_BACKEND_URL}'
  - '.'
